# Assuming a scenario where we want to run a script to deploy code and then open the web-browser with target URL.
import webbrowser

print("Deployment is Completed.")
webbrowser.open("http://google.com")
